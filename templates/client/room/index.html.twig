{% extends 'base.html.twig' %}

{% block title %}Available Rooms - LuxeStay{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/available_rooms.css') }}">
    <style>
        /* تحسينات إضافية لتنسيق الصور */
        .room-card {
            background: white;
            border-radius: 15px;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            box-shadow: 0 10px 20px rgba(0,0,0,0.05);
            transition: transform 0.3s ease;
        }

        .room-image-wrapper {
            width: 100%;
            height: 220px;
            overflow: hidden;
            position: relative;
        }

        .room-image-wrapper img {
            width: 100%;
            height: 100%;
            object-fit: cover; /* لضمان عدم تشوه الصورة */
            transition: transform 0.5s ease;
        }

        .room-card:hover .room-image-wrapper img {
            transform: scale(1.1); /* تأثير زووم عند تمرير الماوس */
        }

        .price-tag {
            position: absolute;
            top: 15px;
            right: 15px;
            background: rgba(10, 1, 1, 0.9);
            padding: 8px 15px;
            border-radius: 8px;
            font-weight: bold;
            z-index: 2;
        }
    </style>
{% endblock %}

{% block body %}
<div class="rooms-gallery">
    <header class="gallery-header">
        <h1>Available Rooms</h1>
        <p>Explore our selection of premium suites and rooms</p>
    </header>

    <div class="room-grid">
        {% for r in rooms %}
            <article class="room-card">
                {# قسم الصورة المضاف #}
                <div class="room-image-wrapper">
                    <div class="price-tag">
                        ${{ r.roomType.pricePerDay }} <span>/ night</span>
                    </div>

                    {% if r.image %}
                        {# تأكد أن r.image هو اسم الملف المخزن في قاعدة البيانات #}
                        <img src="{{ asset('uploads/rooms/' ~ r.image) }}" alt="Room {{ r.roomNumber }}">
                    {% else %}
                        {# صورة افتراضية فخمة في حال لم تكن هناك صورة للغرفة #}
                        <img src="https://beitalwali.com/to_book/junior-suite/" alt="LuxeStay Room">
                    {% endif %}
                </div>

                <div class="room-card-body">
                    <span class="room-type-label">{{ r.roomType.type }}</span>
                    <h2 class="room-title">Room {{ r.roomNumber }}</h2>

                    <div class="room-features">
                        <p>Accommodates up to {{ r.roomType.capacity }} guests. Experience refined comfort with our premium bedding.</p>
                    </div>

                    <a href="{{ path('client_booking_new') }}" class="btn-book">Book This Room</a>
                </div>
            </article>
        {% else %}
            <div class="empty-gallery">
                <span class="icon">🏨</span>
                <h3>No Rooms Available</h3>
                <p>We are currently fully booked. Please check back later.</p>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
