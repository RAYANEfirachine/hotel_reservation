{% extends 'base.html.twig' %}

{% block title %}Payment Successful{% endblock %}

{% block body %}
    <div class="container container-max d-flex justify-content-center">
        <div class="card w-100" style="max-width:720px">
            <div class="card-body text-center">
                <h1 class="h4">Payment Successful</h1>
                <p class="text-muted">Your reservation #{{ reservation.id }} has been paid and confirmed.</p>
                <a href="{{ path('client_booking_my_bookings') }}" class="btn btn-primary">View my bookings</a>
            </div>
        </div>
    </div>
{% endblock %}
