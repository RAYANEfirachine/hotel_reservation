{% extends 'base.html.twig' %}

{% block title %}New Room{% endblock %}

{% block body %}
    <div class="container container-max">
        <div class="mb-3">
            <h1 class="h3">New Room</h1>
            <p class="text-muted">Add a new room to the inventory.</p>
        </div>

        <div class="card">
            <div class="card-body">
                {{ form_start(form) }}
                    <div class="mb-3">{{ form_row(form.roomNumber) }}</div>
                    <div class="mb-3">{{ form_row(form.roomType) }}</div>
                    <div class="d-flex gap-2">
                        <button class="btn btn-primary">Save</button>
                        <a class="btn btn-outline-secondary" href="{{ path('admin_room_index') }}">Back</a>
                    </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
